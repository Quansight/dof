# Dept. of Forestry (Rust)

Goal of this demo project is to showcase:
* checkpointing environments
* pushing/pulling environments (at certain checkpoints)

## Running

To build the project, ensure you have rust installed.

Then run 
```
cargo build
```